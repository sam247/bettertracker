/**
 * Future command modules can register here, e.g.:
 *
 * import { registerCommandProvider } from "@/lib/commands/registry";
 *
 * registerCommandProvider(() => [
 *   {
 *     id: "tool:gsc-explorer",
 *     label: "GSC Explorer",
 *     group: "Tools",
 *     run: (ctx) => ctx.router.push("/tools/gsc"),
 *   },
 * ]);
 */

export {};
